<%_ type.fields.filter(field => (field.relation && (field.relationType === 'manyToMany' || field.relationType === 'oneOnly' || field.relationType === 'oneToOne' || field.relationType === 'manyToOne'))).forEach(field => { %>
import { <%= field.type %> } from "./<%=field.type%>"
<%_}) %>